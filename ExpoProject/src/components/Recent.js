import React from "react";
import { StyleSheet, Text, View } from "react-native";

export const Recent = () => {
  return (
    <View>
      <Text>RECENT</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
